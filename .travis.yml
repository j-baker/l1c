language: c
sudo: false
install: compiler/build/install-deps.sh
script: holmake all
env: PATH=$PATH:$HOME/HOL/bin LD_LIBRARY_PATH=$HOME/polyml/lib LIBRARY_PATH=$HOME/polyml/lib
cache:
  directories:
    - $HOME/HOL
    - $HOME/polyml
notifications:
  email: false
addons:
  apt:
    packages:
    - polyml
    - texlive
    - latexmk
